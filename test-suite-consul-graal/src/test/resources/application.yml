micronaut:
  application:
    name: service-discovery

consul:
  client:
    registration:
      enabled: true
      check:
        deregister-critical-service-after: 30s
    default-zone: ${consul.address}
